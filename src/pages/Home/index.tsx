import * as React from "react";
import ServerListing from "@/components/ServerListing";

const Home: React.FC = () => {
  return (
    <React.Fragment>
      <ServerListing />
    </React.Fragment>
  );
};

export default Home;
